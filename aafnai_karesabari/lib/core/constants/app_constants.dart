/// App-wide constant values: display name, currency label, and the
/// platform's commission rate taken from each completed order.
abstract final class AppConstants {
  static const appName = 'Aafnai Karesabari';
  static const currency = 'NPR';
  static const platformFeeRate = .03;
}
